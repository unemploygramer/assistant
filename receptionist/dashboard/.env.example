# Supabase (same as phone server project)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Required for dashboard auth (browser needs these)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Contact form (optional) — emails go to CONTACT_EMAIL_TO
CONTACT_EMAIL_TO=your@email.com
CONTACT_EMAIL_APP_PASSWORD=your-gmail-app-password

# Stripe (optional) — for Upgrade / $99 mo subscription
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_ID_MONTHLY=price_...   # or STRIPE_PRO_PRICE_ID

# Demo page: phone number shown when no twilio_numbers row has is_demo=true
DEMO_LINE=+15551234567

# Base URL for Stripe redirects (defaults to request origin)
NEXT_PUBLIC_APP_URL=https://your-dashboard-domain.com
